---
import MainLayout from '../layouts/main-layout.astro';
---
<MainLayout title="Carga de ticket">
  <div class="p-4 rounded-md bg-slate-100 mt-10 text-slate-700">
    <form class="grid grid-cols-2 gap-5">

      <div class="col-span-1">
        <div class="mt-2">
          <label for="mail">Mail: </label>
          <br>
          <input id="mail" name="mail" type="text" placeholder="Type here" 
          class="input w-full bg-white" />
        </div>

        <div class="mt-6">
          <label for="sucursal">Código de sucursal: </label>
          <br>
          <input id="sucursal" name="sucursal" type="text" placeholder="Type here" 
          class="input w-full bg-white" />
        </div>

        <div class="mt-6">
            <label for="sucursal">Resumen: </label>
            <br>
            <input id="sucursal" name="sucursal" type="text" placeholder="Type here" 
            class="input w-full bg-white" />
        </div>

      </div>

      <div class="col-span-1 relative">
        <div>
            <label for="detalle">Detalle de la consulta: </label>
            <textarea id="detalle" name="detalle" class="textarea w-full mt-2 bg-white" placeholder="Bio"></textarea>
        </div>
        <div class="border-dashed rounded-md border-4 border-slate-300 px-4 py-2 mt-4">
            <code>Pegar hasta 4 capturas de pantalla</code><br>
            <code>Presione <kbd>Ctrl</kbd> <kbd> + </kbd> <kbd>V 📋</kbd> </code>
        </div>
        <div class="absolute w-full text-end bottom-0">
          <button type="button" class="btn w-1/3">Enviar</button>
        </div>
      </div>

    </form>
  </div>
</MainLayout>